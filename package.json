{
  "name": "in-the-margins",
  "version": "1.0.0",
  "private": true,
  "description": "A personal journal. Made for the everyday.",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "eleventy --serve",
    "build": "node scripts/clean.mjs && eleventy",
    "new": "node scripts/new-post.mjs",
    "check": "node scripts/check.mjs",
    "test": "npm run build && npm run check"
  },
  "devDependencies": { "@11ty/eleventy": "3.1.6" }
}
